let arr = [1, 2,3, 3]
    let set = new Set(arr);
    let uniquarr = Array.from(set);
    console.log(uniquarr);

    // for(let i =1; i<arr.length-1;i++){
    //     arr[i] = uniquarr[i]
    // }

    // console.log(arr)
 