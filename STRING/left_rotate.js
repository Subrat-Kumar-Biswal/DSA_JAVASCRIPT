var rotateString = function(s, goal) {
    const array=s+s;
    if(array.length !== goal.length){
        return false
    }else{
        return false
    }
    // console.log(array);
};

let s = "abcde"
let goal = "cdeab"

console.log(rotateString(s, goal))
